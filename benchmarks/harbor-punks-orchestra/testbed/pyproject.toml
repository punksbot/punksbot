[project]
name = "harbor-punks-testbed"
version = "0.1.0"
description = "Trial provisioning and local Punks stack tooling for harbor-punks-orchestra"
requires-python = ">=3.12"
dependencies = [
    "harbor-punks-orchestra",
    "coincurve>=20",
    "psycopg[binary]>=3.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.optional-dependencies]
dev = [
    "pytest>=8.4",
    "ruff>=0.15",
]

[tool.uv.sources]
harbor-punks-orchestra = { path = "..", editable = true }

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 88
