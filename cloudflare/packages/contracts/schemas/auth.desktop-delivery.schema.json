{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "punks://contracts/auth.desktop-delivery@1",
  "title": "DeliverDesktopSessionCommand",
  "type": "object",
  "additionalProperties": false,
  "required": ["contract", "deliveryToken", "installationId"],
  "properties": {
    "contract": { "const": "auth.desktop-delivery@1" },
    "deliveryToken": { "type": "string", "minLength": 43, "maxLength": 128 },
    "installationId": {
        "type": "string",
        "format": "uuid"
      }
  }
}
