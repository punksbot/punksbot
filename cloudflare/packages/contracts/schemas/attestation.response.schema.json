{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "punks://contracts/attestation.response@1",
  "title": "AttestationResponse",
  "type": "object",
  "additionalProperties": false,
  "required": ["keyVersion", "event"],
  "properties": {
    "keyVersion": { "type": "string", "minLength": 1, "maxLength": 64 },
    "event": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "pubkey", "created_at", "kind", "tags", "content", "sig"],
      "properties": {
        "id": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "pubkey": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "created_at": { "type": "integer", "minimum": 0 },
        "kind": { "type": "integer", "minimum": 50000, "maximum": 59999 },
        "tags": {
          "type": "array",
          "items": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "maxLength": 2048 }
          }
        },
        "content": { "type": "string", "maxLength": 262144 },
        "sig": { "type": "string", "pattern": "^[0-9a-f]{128}$" }
      }
    }
  }
}
