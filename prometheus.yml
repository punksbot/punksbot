# Local dev Prometheus config — scrapes punks-relay metrics.
# The relay runs on the host (not in Docker), so we use host.docker.internal.
# Default metrics port is 9102; override with PUNKS_METRICS_PORT.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: punks-relay
    static_configs:
      - targets: ["host.docker.internal:9102"]
